```{r}
coin_toss <- function() {
    toss <- runif(1)
    if (toss <= 0.5) {
        return("HEADS")
    } else {
        return("TAILS")
    }
}
```

```{r}
exp <- function(x=1){
heads <- 0
for (i in c(1:x)){
    toss <- coin_toss()
    heads <- if_else(toss == 'HEADS' ,heads +1, heads)   
}
return(heads/x)
}

```

```{r}
#c=seq(from =2, to = 10000, by = 20)
c <-  c(1,10,50,100,200,500,1000,2000,5000,10000,50000)
df<- tibble('experiments' = c, 'Prob' = map_dbl(c,exp))
#df<- tibble('experiments' = as.character(c), 'Prob' = map_dbl(c,exp))
```

```{r}
mean(df$Prob)
sd(df$Prob)
```
```{r}

```


```{r}
library(tidyverse)
library(ggplot2)

  ggplot(data = df, aes(x = 'experiments', y= 'Prob'))+
  geom_col()
  
  #why???

```

